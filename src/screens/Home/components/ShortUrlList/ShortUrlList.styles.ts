import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    borderColor: '#ccc',
    borderWidth: 2,
    borderRadius: 10,
    padding: 15,
  },
  flatlistHeaderText: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  headerTitleContainer: {
    marginBottom: 5,
  },
});
